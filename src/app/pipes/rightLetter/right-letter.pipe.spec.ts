import { RightLetterPipe } from './right-letter.pipe';

describe('RightLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new RightLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
